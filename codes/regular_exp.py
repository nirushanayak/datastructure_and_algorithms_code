import re
#
# word = "/migen002/sdf"
#
# # vlg = re.search(r'/(.+?)/', word).group(1)
# # print(vlg)
# def pattern():
#     pat = '(m|u)(i|p|sp|z|sz)(gen|ora|esx|con)\d{3}'
#     try:
#         pattern = re.findall(pat, re.search(r'/(.+?)/', word).group(1))
#         return ''.join(pattern[0])
#     except:
#         print("invalid")
#
# print(pattern())

a = ('value'
    if False
    else 'false')
print(a)
